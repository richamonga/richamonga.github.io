SELECT STUDENTS.NAME
FROM STUDENTS 
JOIN FRIENDS ON STUDENTS.ID=FRIENDS.ID
JOIN PACKAGES SALARY_STUDENT ON SALARY_STUDENT.ID=STUDENTS.ID
JOIN PACKAGES SALARY_FRIEND ON SALARY_FRIEND.ID=FRIENDS.ID
WHERE SALARY_FRIEND.SALARY > SALARY_STUDENT.SALARY
ORDER BY SALARY_FRIEND.SALARY ;
